class Tuple {
    constructor(key,perm)
    {
        this.key = key;
        this.perm = perm;
    }
    getEval(num)
    {
        var i = 0;
        for(;i < key.length;i++)
        {
            if(num == key[i])
            {
                console.log("Tuple:",eval[i],"\n");
                return eval[i];
            }
        }
    }
}
var uniqueSort = new Tuple(key, eval);